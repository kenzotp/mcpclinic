{
  "name": "mcpclinic",
  "version": "0.1.0",
  "private": true,
  "description": "MCP Clinic — agent-readiness probing, reporting and the mcp-basis template server",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "probe-mcp": "tsx src/cli.ts probe-mcp",
    "probe-surface": "tsx src/cli.ts probe-surface",
    "rank": "tsx src/cli.ts rank",
    "test": "tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
