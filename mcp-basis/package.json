{
  "name": "mcp-basis",
  "version": "0.1.0",
  "description": "MCP Clinic's template for production MCP servers: scoped auth, tenant isolation, idempotent writes, destructive-action confirmation, audit log. Configure per client.",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "tsx src/index.ts",
    "upstream": "node demo/upstream-demo.mjs",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "tsx": "^4.19.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
